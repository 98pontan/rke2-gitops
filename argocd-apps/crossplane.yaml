# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: crossplane
#   namespace: argocd
#   finalizers:
#     - resources-finalizer.argocd.argoproj.io
# spec:
#   project: default
#   source:
#     repoURL: 'https://charts.upbound.io/stable'
#     chart: crossplane
#     # Matches the '--devel' requirement by grabbing the latest v2.x enterprise-grade distribution
#     targetRevision: 2.* helm:
#       # You can add custom helm parameters here if needed
#       values: |
#           args:
#             - "--debug"
#   destination:
#     server: 'https://kubernetes.default.svc'
#     namespace: crossplane-system
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true
